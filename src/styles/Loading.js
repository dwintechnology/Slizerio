import { makeStyles } from '@mui/styles'

const useStyle = makeStyles({
    gif: {
        width: '9%',
        position: 'fixed',
        left: '50%',
        top: '41%',
        marginLeft: '-4.5%',
    },
}, {
    name: 'Loading'
})

export default useStyle